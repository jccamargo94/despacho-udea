services:
  cli:
    build: .
    volumes:
      - ./data:/app/data
    command: ["run"]

  api:
    build: ./services/api
    profiles: ["future"]

  worker:
    build: ./services/worker
    profiles: ["future"]
